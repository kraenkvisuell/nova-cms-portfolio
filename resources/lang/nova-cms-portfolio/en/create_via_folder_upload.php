<?php

return [
    'headline' => 'Create/update Projects by uploading a folder',
    'intro' => 'Select a folder which contains:<br>one or multiple folders representing <strong>categories</strong> (the folder names being the category title).<br>Within these category folders must be one or multiple folders representing <strong>projects</strong> (the folder names being the project title). Theses project folders must contain the image files.<br><br>The window will reload when all files are uploaded - <strong>Do not close or reload the window while the upload is in progress</strong><br>(but if nothing happens for a few minutes, you can reload.)',
    'new_category' => '... or create a new category',
];
